import { ActionTree } from 'vuex';
import { SearchState } from './types';
import { RootState } from '@/store/types';

export const actions: ActionTree<SearchState, RootState> = {
  // hier searchresults ophalen en opslaan?
};
